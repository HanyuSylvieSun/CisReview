<%= form_with(model: review, local: true) do |form| %>
  <% if review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :difficulty %>
    <%= form.number_field :difficulty, id: :review_difficulty %>
  </div>

  <div class="field">
    <%= form.label :workload %>
    <%= form.number_field :workload, id: :review_workload %>
  </div>

  <div class="field">
    <%= form.label :quality %>
    <%= form.number_field :quality, id: :review_quality %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment, id: :review_comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
